class SignUpValidator {
  // static String? validatePhone(String phone) {
  //   final err = ValidatorsHelper.getQatarPhoneErrorMessage(phone);
  //   return err.isEmpty ? null : err;
  // }

  // static bool isPhoneValid(String phone) => validatePhone(phone) == null;
  // static String getPhoneErrorMessage(String phone) => validatePhone(phone) ?? '';

  // static String? validateEmail(String email) {
  //   final err = ValidatorsHelper.getEmailErrorMessage(email);
  //   return err.isEmpty ? null : err;
  // }

  // static bool isEmailValid(String email) => validateEmail(email) == null;
  // static String getEmailErrorMessage(String email) => validateEmail(email) ?? '';
}
