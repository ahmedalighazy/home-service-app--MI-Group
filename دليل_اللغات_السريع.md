# 📱 دليل نظام اللغات - نسخة عربية سريعة

## الملخص السريع

تطبيقك **يدعم اللغتين العربية والإنجليزية بشكل كامل** ✅

---

## 🌍 كيف يعمل النظام

### عند فتح التطبيق:
1. يتم اكتشاف لغة الجهاز تلقائياً
2. أو يتم استرجاع اللغة المحفوظة السابقة
3. الشاشة تعرض باللغة الصحيحة

### عند تبديل اللغة:
1. المستخدم يختار لغة جديدة
2. يتم حفظها تلقائياً
3. جميع الشاشات تتحدث فوراً ✨

---

## 💻 الملفات الرئيسية

```
📁 core/language/
   └── language_cubit.dart         ← إدارة اللغة

📁 core/utils/l10n/
   └── app_strings.dart            ← جميع الترجمات (200+)

📁 core/utils/helpers/
   ├── cache_helper.dart           ← حفظ اللغة
   └── language_test_helper.dart   ← اختبار اللغات

main.dart                           ← إعدادات التطبيق
```

---

## 🚀 كيفية استخدام اللغات في شاشة جديدة

### الطريقة السهلة (3 خطوات فقط):

```dart
import 'package:flutter/material.dart';
import 'package:home_service_app/core/utils/l10n/app_strings.dart';

class MyNewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 1️⃣ اكتشف اللغة
    final isArabic = Localizations.localeOf(context).languageCode == 'ar';
    
    // 2️⃣ طبق الاتجاه
    return Directionality(
      textDirection: isArabic ? TextDirection.rtl : TextDirection.ltr,
      child: Scaffold(
        body: Center(
          // 3️⃣ استخدم الترجمات
          child: Text(AppStrings.emailLabel),
        ),
      ),
    );
  }
}
```

**انتهى! 🎉**

---

## 📝 كيفية إضافة ترجمات جديدة

### خطوة واحدة فقط!

في ملف `lib/core/utils/l10n/app_strings.dart`:

```dart
// أضف هذا السطر
static String get myNewText => _isArabic 
    ? 'النص العربي'
    : 'English Text';
```

ثم استخدمه في أي مكان:
```dart
Text(AppStrings.myNewText)  // مترجم تلقائياً ✨
```

---

## 🔧 تبديل اللغة برمجياً

```dart
// تبديل إلى العربية
context.read<LanguageCubit>().setArabic();

// تبديل إلى الإنجليزية
context.read<LanguageCubit>().setEnglish();

// تبديل بين اللغتين
context.read<LanguageCubit>().toggleLanguage();
```

---

## ✅ قائمة التحقق للشاشات الجديدة

```
[ ] import Localizations و AppStrings
[ ] أنشئ متغير isArabic
[ ] غطِّ الشاشة بـ Directionality
[ ] استخدم AppStrings للنصوص
[ ] اختبر مع اللغة العربية
[ ] اختبر مع اللغة الإنجليزية
```

---

## 🎨 الشاشات المدعومة

| الشاشة | اللغات | ملاحظة |
|-------|--------|--------|
| Splash | ✅ | تلقائي |
| Onboarding | ✅ | ديناميكي |
| Sign In | ✅ | RTL/LTR |
| Sign Up | ✅ | RTL/LTR |
| OTP | ✅ | RTL/LTR |
| Complete Profile | ✅ | RTL/LTR |
| Forget Password | ✅ | RTL/LTR |
| و 3 شاشات أخرى | ✅ | RTL/LTR |

---

## 🐛 أخطاء شائعة

### ❌ الخطأ: النصوص لا تتغير عند تبديل اللغة

**الحل**: استخدم `Localizations` بدلاً من متغير عادي

```dart
// ❌ خطأ
final isArabic = _savedLang == 'ar';

// ✅ صحيح
final isArabic = Localizations.localeOf(context).languageCode == 'ar';
```

### ❌ الخطأ: النص العربي بالمقلوب

**الحل**: استخدم `Directionality`

```dart
return Directionality(
  textDirection: isArabic ? TextDirection.rtl : TextDirection.ltr,
  child: // محتوى
);
```

### ❌ الخطأ: ترجمة مفقودة

**الحل**: أضفها في `app_strings.dart`

```dart
static String get missingKey => _isArabic 
    ? 'النص'
    : 'Text';
```

---

## 📊 الإحصائيات

- **عدد الترجمات**: 200+
- **اللغات**: عربي + إنجليزي
- **الشاشات المدعومة**: 10+
- **نسبة التغطية**: 100% ✅

---

## 🎯 أهم النقاط

1. **استخدم `AppStrings` دائماً** للنصوص
2. **استخدم `Directionality`** لكل شاشة
3. **اختبر اللغتين** قبل الإطلاق
4. **احفظ `isArabic` في متغير** تجنباً للتكرار

---

## 📞 المساعدة السريعة

| المشكلة | الحل |
|--------|------|
| الشاشة لا تتغير عند تبديل اللغة | استخدم `Localizations.localeOf(context)` |
| النص العربي معكوس | أضف `Directionality` مع `TextDirection.rtl` |
| ترجمة مفقودة | أضفها في `app_strings.dart` |
| اللغة لا تحفظ | تأكد من استخدام `CacheHelper.saveData` |

---

## 🎓 الخلاصة

```
✨ نظام احترافي
✨ سهل الاستخدام
✨ 100% عربي + إنجليزي
✨ جاهز للإنتاج
```

---

**احمِ مشروعك وتطور بثقة! 🚀**

*آخر تحديث: 5 يونيو 2026*
